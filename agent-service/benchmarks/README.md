# Offline token benchmark

Runs the existing 50 local evaluation cases with the Fake Backend only. The baseline deliberately includes all registered schemas, ten verbose historical turns and a raw representative result. The optimized path uses the real deterministic selector, six safe history summaries and minimized result shape. Results are generated by `python -m benchmarks.report`; no network or model call occurs.
