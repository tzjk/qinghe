# 本地部署说明（M1 初稿）

本项目仅提供本地运行说明，不操作系统服务。

## 服务端

后端工程位于 `../backend`，使用 Maven 构建，服务端口为 8090。启动前请确认本地数据库和 Redis 可用，并通过环境变量提供连接密码。

## 前端

前端工程位于 `../frontend`，使用 npm 安装依赖和构建，开发服务器端口为 5174。

初始化 SQL 仅供人工确认后导入；项目不会自动导入或执行 SQL。
